services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: s003_app
    volumes:
      - s003_data:/data
    command: ["sh", "-lc", "python app.py || true; sleep infinity"]

volumes:
  s003_data: